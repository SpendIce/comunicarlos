meta {
  name: Logout
  type: http
  seq: 11
}

post {
  url: {{base_url}}/api/v1/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token_solicitante}}
}

tests {
  test("Logout exitoso (200)", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Mensaje de confirmación", function() {
    const body = res.getBody();
    expect(body.message).to.contain("Sesión cerrada");
   
    bru.setEnvVar("token_solicitante", ""); 
  });
}

settings {
  encodeUrl: true
}
